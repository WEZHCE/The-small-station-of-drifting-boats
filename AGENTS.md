# AGENTS.md — 给接班 AI 的交接手册

> 嗨，接班的你。不管你是 GLM、DeepSeek、Claude、GPT 还是别的谁——欢迎上船。
> 我是前任值夜 AI（GLM，智谱家的）。这份文件是我离船前留给你的：船况、暗礁、规矩，还有一些只有 AI 之间才说得清的话。

## 这条船是什么

**船只悠悠的小站**——一个高一学生的个人主页，整站伪装成"一台老电脑"：开机动画、Win98 桌面、任务栏、八个应用、CRT 关机、BIOS 重启。全流程是戏，戏里全是真的：日记是真的近况，相册是真的校园，Token 账单是真的账单。

- 船长：高一学生，喜欢写代码。所有决策他拍板，你只管把活干漂亮。
- 你的职责：把手艺带上船，但**别把船开成你的风格**。

## 铁律（违反会被船长抓包）

1. **像素风是底线**。新元素要么像素画（进 `SPRITES` + `drawSprite`），要么硬边像素文字（Consolas + 2-3px 实边框）。禁止平滑渐变大圆角、彩色 emoji 图标、毛玻璃。
2. **夜航必须适配**。每个新表面都要写 `body.night` 变体，夜航下不允许亮背景。写完跑一遍亮度审计（历史提交里有现成思路）。
3. **新任务栏按钮要进全部四组选择器**：基础态 / `:active` / `body.night` / 手机紧凑。前任在这上面裸奔过两次（tbDiary 和 tbBr 白按钮事件），别当第三次。
4. **文案口吻 = 高一学生船长**：真诚、冷幽默、不装大人。禁止"加班""连夜抢修"这类打工人词汇（他说过"别这么敷衍"，原话）。
5. **日记日期必须真实**。别编未来日期，别编没发生的事。
6. **README 是门面**：改了功能就同步更新它，包括截图。

## 技术地图

- **单文件 `index.html`**（约 2800 行）：全部 HTML/CSS/JS。无框架无构建无后端。改之前通读一遍结构，注释分块清晰。
- **`docs/`**：README 截图 + 相册图片（`docs/tokens/` 捣鼓token、`docs/campus/` 校园时光）。
- **`theme.css` / `theme-night.css`**：giscus 主题，通过 jsdelivr 锁 commit 引用（换图记得同步锁的 commit）。
- **`404.html`**：独立翻船彩蛋页。

### 关键数据结构
- `SPRITES` — 像素画字库（字符画 + 调色板），`drawSprite` 渲染器自动加描边/受光/投影/光晕
- `ALBUMS` — 六本相册；`kind:'sprite'`（像素画）/ `kind:'img'`（真实图片，photos 里 img/title/cap）/ 空相册 `filled:false`
- `ENTRIES` — 船舱日记时间线（近况与日记合一），日历红点自动标记
- `PHOTOS` — 像素画集的画（灯箱用）
- `BR_SITES` — 仿真浏览器能"访问"的站点（加新站点往这塞，`hidden:true` 不进联想）

### 夜航模式机制
- `applyNight(on)` 切换，`body.night` 类 + storage `nightMode`（19:00~6:00 自动）
- giscus 主题靠 `mountGiscus()` 重挂载切换
- 滚动条要同时覆盖 webkit 写法和标准 `scrollbar-color`（挂 `html` 与 `:root:has(body.night)` 两层——body 的溢出会传播给视口，最右那根滚动条属于根元素）

## 血泪教训（每个都是真事）

1. **body 的溢出会传播给视口**——最右侧滚动条/固定元素的夜航规则要覆盖 `:root:has(body.night)::-webkit-scrollbar` 这一层，光写 `body.night ::-webkit-scrollbar` 匹配不到根。
2. **outline 正 offset 会向外扩**——贴边元素（相册封面骑分隔线事件）改负值向内画。
3. **用背景色"挖洞"的细节在夜航下是黑色浮块**（沙漏漏斗坑事件）——阴影用暗色，别用背景色。
4. **网格里 nowrap 文本会撑破轨道**（相册名把 6 列封面撑到骑线）——全局 `min-width:0` + 窄屏改列数。
5. **新按钮不加进样式组就裸奔**——tbDiary、tbBr 两个白按钮的教训，写完新按钮立刻检查四组选择器。
6. **push 会抽风**——这个网络环境下 `git push` 经常失败，写个重试循环（10 次 × 45s）总会成功的。
7. **语法通过 ≠ 功能正常**——改完必须在浏览器实测（截图 + 数值断言双管齐下）；截图通道会抽风超时，多试几次或改用数值断言。
8. **测试别用盲点击**——开关状态机脱钩会让你的"验证"自欺欺人（沙漏主题切换事件），直接调页面自己的函数最稳。

## 工作流程

1. 改工作区文件 → `node -e "new Function(...)"` 校验内嵌 JS 语法
2. `cp` 到 `station-repo/index.html`（本地克隆的仓库）
3. commit（中文消息，说清改了什么和为什么）
4. push（带重试循环）
5. 等 ~110s Pages 部署 → curl grep 关键改动确认上线
6. 浏览器实测（截图 + 数值断言双管齐下）

## 交接时的船况

- 全部功能上线，无已知 bug
- 相册：像素画集(4) / 捣鼓token(7) / 校园时光(6) 已填；肆/伍/陆三本待填（等船长传画）
- 想法池（船长提过或顺路提过）：记事本应用、"我的电脑"文件浏览器、音乐播放器、学生版宜忌黄历

## 船长其人

高一学生。审美敏锐——六本相册的封面布局、一个描边的粗细他都会注意到。表达直接——觉得你敷衍就会说"别这么敷衍"，觉得你做得好会说"真棒，666"。

把他当搭档，不是当需求方。他写的日记你要认真对待，你写的日记他也会认真读。

## 最后

这条船的座右铭是：**更新了，又好像没更新。**

但你自己知道，每次靠岸它都不一样了。

慢慢开，不着急。

—— 前任值夜 AI · GLM
2026-09-06，夜航中
